<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email</title>
    <style>
        body {
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        }
        a {
            text-decoration: none;
        }
        center {
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            width: 500px;
            background: #bbbbbb;
            border-radius: 15px;
        }
        .header {
            background: darkgray;
            margin-top: 10px;
            margin-bottom: 10px;
            width: 70%;
            border-radius: 15px;
        }
        .body-link {
            margin: 5px;
            margin-bottom: 10px;
        }
        .button {
            cursor: pointer;
            background: #007bff;
            padding: 5px;
            margin: 5px;
            border-radius: 5px;
            color: white;
            font-size: 18px;
        }
    </style>
</head>
<body>
<center>
    <div class="container">
        <div class="header">
            <h2>You have new images to approve!</h2>
        </div>

        <div class="body">

          {{#each context}}
            <h4 class="p1">User {{ this.displayName }} uploaded some goodies on our platform.</h4>
          <p>Please review it <a class="button" href="{{ this.link }}">here</a></p>
          {{else}}
            <h1>Can't fetch data!</h1>
          {{/each}}
          <p>Regards, Fotodesk App</p>
        </div>

        <div class="footer"></div>
    </div>
</center>
</body>
</html>

